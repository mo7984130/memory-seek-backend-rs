[package]
name = "types-db-api"
version = "0.1.0"
edition = "2024"
description = "实体注册契约: schema 前缀与初始化回调"

[dependencies]
common.path = "../../common"

linkme.workspace = true
sea-orm = { workspace = true, features = ["sqlx-postgres", "macros"] }
