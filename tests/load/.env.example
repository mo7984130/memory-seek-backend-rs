REMOTE_HOST=        # 必填，远程服务器地址（如 192.168.1.100 或域名）
REMOTE_USER=        # 必填，SSH 用户名
SSH_KEY=            # 可选，SSH 私钥路径（不填则用密码认证）
SERVER_BIN_PATH=    # 可选，server binary 路径（本地和 CI 都可传入）
SERVER_PORT=7985    # 服务端口，默认 7985
REMOTE_DIR=/tmp/memory-seek-server/loadtest  # 远程部署目录
