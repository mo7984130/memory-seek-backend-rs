[package]
name = "email"
version = "0.1.1"
edition = "2024"

[features]
metrics = ["dep:metrics"]

[dependencies]
common = { path = "../../common" }
lettre = { workspace = true }
metrics = { workspace = true, optional = true }
